When trying to run `pnpm test`, we get the error message:

```
➜ pnpm test
> pnpm-prettier-glsl@1.0.0 test /Users/paras/workspace/pnpm-prettier-glsl
> prettier --write shader.frag

shader.frag
[error] Couldn't resolve parser "glsl-parser"
```
